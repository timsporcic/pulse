# Generated by OpenTofu - see infra/templates/litestream.yml.tftpl

dbs:
  - path: /var/lib/pulse/pulse.db
    replicas:
      - type: s3
        bucket: ${bucket}
        path: pulse.db
        endpoint: https://${region}.digitaloceanspaces.com
        region: ${region}
