# Generated by OpenTofu - see infra/templates/Caddyfile.tftpl

${domain} {
	@metrics path /metrics
	respond @metrics 403

	basic_auth {
		import /etc/caddy/pulse-users
	}

	reverse_proxy 127.0.0.1:7070

	encode gzip

	log {
		output stdout
		format json
	}
}
