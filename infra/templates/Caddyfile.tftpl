# Generated by OpenTofu - see infra/templates/Caddyfile.tftpl

${domain} {
	@metrics path /metrics
	respond @metrics 403

	reverse_proxy localhost:7070

	encode gzip

	log {
		output stdout
		format json
	}
}
